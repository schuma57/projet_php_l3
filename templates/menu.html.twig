
<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="index.php">Cocktails</a>
        </div>

        <div class="collapse navbar-collapse" id="collapse-1">
            <ul class="nav navbar-nav">

                <li><a href="cocktails.php">
                    <span class="glyphicon glyphicon-glass"></span> Toutes les recettes </a></li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Ingrédients <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {% set truc = session.hierarchie.Aliment['sous-categorie'] %}
                        {% for key, item in session.hierarchie.Aliment['sous-categorie'] %}
                            <li> {{ item }} </li>
                        {% endfor %}
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">un truc <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {% for test in truc %}
                            <li> {{ test }}
                            <ul>
                            {% for bidule in session.hierarchie[test]['sous-categorie'] %}
                                <li> {{ bidule }} </li>
                            {% endfor %}
                            </ul>
                            </li>
                        {% endfor %}
                    </ul>
                </li>

            </ul>
        
            <form class="navbar-form navbar-left" role="search">
                <div class="form-group">
                <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">
                    <span class="glyphicon glyphicon-ok"></span> Rechercher</button>
            </form>
            
            <ul class="nav navbar-nav navbar-right">
                
                {% if session.pseudo is defined %}
                
                <li class="dropdown">    
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <button class="btn btn-warning">
                            <span class="glyphicon glyphicon-cog"></span> </button> </a>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="#">
                            <span class="glyphicon glyphicon-user"></span> Profile</a></li>
                        <li><a href="#">
                            <span class="glyphicon glyphicon-edit"></span> Changer password</a></li>
                        <li><a href="#">
                                <span class="glyphicon glyphicon-star"></span> Mes favoris</a></li>
                        <li class="divider"></li>
                        <li> <a href="deconnexion.php">
                            <span class="glyphicon glyphicon-log-out"></span> Deconnexion </a> </li>
                    </ul>
                </li>
                
                {% else %}
                <li> <a href="connexion.php">
                    <span class="glyphicon glyphicon-log-in"></span> Connexion </a> </li>
                    
                {% endif %}
            </ul>
        </div> <!-- /.navbar-collapse -->
    </div> <!-- /.container-fluid -->
</nav>
